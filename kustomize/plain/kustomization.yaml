apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# A comment that must survive every edit Radar makes to this file.
resources:
  - configmap.yaml
  - second.yaml
patches:
  - path: configmap-patch.yaml
  - path: second-patch.yaml
    target:
      kind: ConfigMap
      name: second
